# Handoff: 11 Sep 2026 session (ops simplification + backlog cleanup)

નવા session માટે. પહેલા `docs/ops-simplification-plan.md` અને `docs/week1-edits.md` વાંચો.

## શું થયું

1. **ઑડિટ પૂરો.** n8n ના 132 એક્ટિવ વર્કફ્લો (116 વાંચ્યા, 16 MCP માં બંધ), Directus, Telegram ચેનલો. તારણ પ્લાનમાં છે.
2. **n8n MCP થી production વર્કફ્લો બદલવા સલામત નથી.** `update_workflow` આખો વર્કફ્લો ફરી લખે છે, `get_workflow_details` credentials બતાવતું નથી, અને credentials type પ્રમાણે auto-assign થાય છે (probe workflow `srMIW9Bk4bZKukIt`, archived). એટલે Week 1 ના ફેરફાર UI માં હાથે કરવાના છે (`docs/week1-edits.md`) અથવા n8n REST API થી (key + network allow જોઈએ; container માંથી `automation.ratguards.com` block છે).
3. **Backlog cleanup શરૂ, અધૂરું.** Dry run ચાલુ હતું, કોઈ ડેટા લખ્યો નથી.

## Backlog: ખરી હાલત (11 Sep)

| જૂથ | ઓર્ડર | નોંધ |
|---|---|---|
| lifecycle NDR_OPEN | 560 | બધા orders.status=delivered (Shopify fulfilled), lifecycle_source=shopify, 552 ને ndr row કે shipment row નથી. મોટા ભાગના Jul 2026. Backfill artefact. |
| DISPATCHED | 291 | મોટા ભાગના Jan–May 2026, status delivered |
| IN_TRANSIT | 339 | 314 Sept ના, મોટા ભાગના ખરેખર transit માં. છેડવા નહીં સિવાય Shopify delivered કહે |
| CONFIRMED | 126 | 90 Aug ના; 62 status delivered, 64 confirmed |
| MANIFESTED / OUT_FOR_DELIVERY / PLACED / ROUTED | 30 / 40 / 11 / 3 | |
| lifecycle null | 485 | 169 ને merged_into સેટ છે (duplicate merge), 349 Aug ના |
| open exceptions | 275 | ROUTE_STALLED 112 (order CONFIRMED), NDR_UNTOUCHED 72 (23 નો order DELIVERED), CONFIRM_STALLED 37 |

`orders.status='delivered'` એટલે Shopify માં fulfilled, delivery નું proof નહીં (Safety Net નો FULFILLMENT_MAP 'fulfilled' → 'delivered').

## Cleanup ની રીત (નક્કી કરેલી)

1. **Truth source = Shopify.** દરેક stale ઓર્ડર માટે GraphQL: `orders(first:1, query:"name:NNNN") { edges { node { name cancelledAt displayFulfillmentStatus tags fulfillments(first:3) { status displayStatus createdAt deliveredAt inTransitAt trackingInfo { number company } } } } }` (validated). 40 alias per call. (Session /tmp માં parital results હતા, નવા session માં ફરી લેવા પડશે.)
2. **નિયમ:** `deliveredAt` અથવા `displayStatus=DELIVERED` → DELIVERED (occurred_at = deliveredAt); `cancelledAt` → CANCELLED; proof ન હોય → ન બદલવું, અલગ યાદીમાં બતાવવું.
3. **લખવાનો રસ્તો = Lifecycle Event Writer** (`HDzhoVBNZfSN8dmV`), સીધું orders patch નહીં. એના માટે temporary bridge workflow બનાવ્યો છે: **`WAnRdwamCjOcMRZX` "Ratguard - Lifecycle Bridge (backlog cleanup, temporary)"**, inactive. `execute_workflow` (executionMode manual, type webhook, POST body `{secret, events:[...]}`) થી ચાલે છે, wrong secret નો test પાસ થયો (execution 228315). Secret એ workflow ના "Check Secret And Shape" Code node માં છે. 100 events per call. Events: `{to_state, order_id, order_number, occurred_at, awb_number?}`; source `backlog_cleanup`, dedupe_key `backlog:<order_id>:<state>`. **કામ પૂરું થાય પછી આ workflow archive કરવો.**
4. **Exceptions:** ઓર્ડર terminal (DELIVERED/CANCELLED/RTO_RECEIVED) થાય પછી તેના open exceptions ને Directus માં `status=auto_resolved, resolved_at, resolution_note` (Ops Action ની જેમ) કરવા. Directus MCP થી સીધું થાય છે.
5. **merged_into વાળા 169 orders:** છેડવા નહીં, user ને પૂછવું.
6. **Lifecycle Backfill** (`0DBk1QROSxHCA6Qp` inactive) ચલાવવો? ના, એણે જ NDR_OPEN artefacts બનાવ્યા લાગે છે. પહેલા Shopify-truth cleanup.

## પેન્ડિંગ / user પાસે

- Week 1 UI edits (`docs/week1-edits.md`).
- n8n API key + network allow (environment settings: "Default · mkaklotar7711" chip → gear → Environment variables `N8N_API_KEY`, Network access Custom + `automation.ratguards.com`).
- Hardcoded bot token `8929163328:...` 5 વર્કફ્લોમાં, revoke કરવો.
- 16 workflows માં "Available in MCP" ચાલુ કરવું.
- Gold Set Harness (TEST ONLY) એક્ટિવ છે.
